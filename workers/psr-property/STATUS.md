# psr-property

The lean application tree is now in this repository: `app/`, `worker/`, `lib/`, `components/`, `data/`, `scripts/`, `tests/`, package files, and the Wrangler configs.

`public/` static assets are not in git. They are served from R2 and the live site.

Cloudflare Builds auto-deploy is not enabled. The release gate in `docs/cloudflare-builds.md` still applies.

Espacios map Workers and map assets are not part of this tree.
