# psr-shell-events

Role recorded in `workers/README.md`: events shell routing.

This directory is a stub. The repo has no Wrangler config and no source for this Worker, so **no routes are declared here**.

Known reference: `wrangler.media-edge.jsonc` binds service `SHELL` to `psr-shell-events`, environment `production`. This repo does not declare a separate route list for `psr-shell-events`.

A Worker named `psr-shell-events` exists on the Cloudflare account. Its route list is not copied into this repo.
