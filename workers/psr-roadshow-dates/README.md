# psr-roadshow-dates

Role recorded in `workers/README.md`: Singapore roadshow date HTML rewrite.

This directory is a stub. The repo has no Wrangler config and no source for this Worker, so **no routes are declared here**.

Known reference: `wrangler.media-edge.jsonc` binds service `ROADSHOW` to `psr-roadshow-dates`, environment `production`. Roadshow patterns in that file are routes of `psr-media-edge`, not routes of this Worker.

A Worker named `psr-roadshow-dates` exists on the Cloudflare account. Its route list is not copied into this repo.
